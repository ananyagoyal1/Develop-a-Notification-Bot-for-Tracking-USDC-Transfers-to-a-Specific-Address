type Transfer @entity {
  id: ID!
  from: String!
  to: String!
  value: BigInt!
  timestamp: BigInt!
  transactionHash: String!
  blockNumber: BigInt!
}

type TransferStats @entity {
  id: ID!
  totalTransfers: BigInt!
  totalValue: BigInt!
  lastUpdateTime: BigInt!
}